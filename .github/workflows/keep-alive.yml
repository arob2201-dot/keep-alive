name: Keep Alive
on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:
jobs:
  keep_alive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: gautamkrishnar/keepalive-workflow@master
